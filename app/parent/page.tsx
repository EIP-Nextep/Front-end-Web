"use client"; // Indispensable pour les composants interactifs
import ParentDashboard from '@/components/Parent/parentDashboard'

export default function ParentPage() {
  return (
    <div>
      <ParentDashboard />
    </div>
  );
}